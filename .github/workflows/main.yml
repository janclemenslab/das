name: Python Package using Conda

on: [push]

# jobs:
#   build:
#       name: Tests (${{ matrix.python-version }}, ${{ matrix.os }})
#       runs-on: ${{ matrix.os }}
#       strategy:
#         fail-fast: false
#         matrix:
#           os: ["ubuntu-latest", "macos-latest", "windows-latest"]
#           python-version: ["3.7", "3.8"]
#       steps:
#       - uses: actions/checkout@v2
#       - name: Setup conda
#           uses: s-weigand/setup-conda@v1
#           with:
#             update-conda: true
#             python-version: ${{ matrix.python-version }}
#             conda-channels: anaconda, conda-forge
#       - run: conda --version
#       - run: which python
#       - name: Display Python version
#         run: $CONDA/bin/python -c "import sys; print(sys.version)"
#       - name: Install package
#         run: |
#           # $CONDA is an environment variable pointing to the root of the miniconda directory
#           $CONDA/bin/conda env create --file env/deepss_gui.yml --name base
#       - name: Test with pytest
#         run: |
#           conda install pytest
#           $CONDA/bin/pytest

# jobs:
#   build-and-run:
#     steps:
#     - uses: actions/checkout@v2
#     - name: Update Conda environment with "requirements.yml"
#       uses: matthewrmshin/conda-action@v1
#       with:
#         args: conda env update -f ./requirements.yml
#     - name: Run "pytest" with the Conda environment
#       uses: matthewrmshin/conda-action@v1
#       with:
#         args: pytest
#     - name: Test cli
#       uses: matthewrmshin/conda-action@v1
#       run: |
#         dss-train --help
#         dss-predict --help
#         xb --help



jobs:
  build:
    name: test_cli (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.7", "3.8"]
    steps:
      - uses: actions/checkout@v2
      - uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          activate-environment: dss
          environment-file: env/deepss_gui.yml
          python-version: ${{ matrix.python-version }}
      - name: install libsndfile
        if: runner.os == 'Linux'
        shell: bash -l {0}
        run: sudo apt-get install libsndfile1
      - name: Conda info
        run: conda info
      - name: Conda list
        shell: bash -l {0}
        run: conda list
      # - name: Install deepss_gui
      #   run: conda env update -f ./env/deepss_gui.yml
      - name: Test with pytest
        shell: bash -l {0}
        run: |
          conda install pytest -n dss
          conda install pytest -n base
          python -m pytest
      - name: Test cli
        shell: bash -l {0}
        run: |
          python -m dss.train --help
          xb --help
      - name: Test cli2
        shell: bash -l {0}
        run: |
          dss-train --help
          dss-predict --help

