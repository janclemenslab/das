name: das-nogui-env
channels:
  - defaults
  - conda-forge
  - ncb
  - apple
dependencies:
  - python=3.9.7
  - zarr
  - pip
  - flammkuchen
  - xarray
  - defopt
  - yaml
  - pyyaml
  - git
  - h5py
  - numpy
  - scipy
  - scikit-learn
  - pyyaml
  - peakutils
  - matplotlib
  - matplotlib-scalebar
  - pandas
  # - librosa
  - peakutils
  - colorcet
  - keras-tuner
  - colorcet
  - keras-tuner
  - tensorflow-deps
  - pandas=1.3.3  # pin
  - pip:
    - kt-legacy
    - pyside6
    - pyqtgraph
    - tensorflow-macos #=2.5
    - tensorflow-metal

# mamba env create -n das_dev2 -f env/das-nogui-osxarm-env.yml
# mamba activate das_dev2
# mamba install librosa -c conda-forge -y # <- install after env was created and pip deps were installed
# pip install -e . --no-dependencies
# cd ../xarray-behave
# pip install -e . --no-dependencies
# pip install samplestamps pyvideoreader
# mamba install scikit-image